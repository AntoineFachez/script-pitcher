# -----------------------------------------------------------------------------
# 1. GENERATED ARTIFACTS AND BUILD OUTPUTS
# -----------------------------------------------------------------------------

# Next.js Build and Cache
# Ignore the Next.js build output directory and the static export directory.
/.next/
/out/
/build

# Typescript Artifacts
# Ignore the TypeScript build cache and auto-generated declaration files.
*.tsbuildinfo
next-env.d.ts

# -----------------------------------------------------------------------------
# 2. DEPENDENCIES AND PACKAGE MANAGER FILES
# -----------------------------------------------------------------------------

# Node Package Manager Folders (Yarn / npm)
/node_modules

# Yarn Plug-n-Play (PnP)
# Ignore PnP files which manage dependency linking.
/.pnp
.pnp.*

# Yarn 2+ Cache and Internal Files
# Ignore all of .yarn/ except for necessary configuration files.
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# -----------------------------------------------------------------------------
# 3. ENVIRONMENT SECRETS AND SENSITIVE FILES
# -----------------------------------------------------------------------------

# Environment Variables
# These files contain secrets (API keys, connection strings) and should never be committed.
.env*
*serviceaccount*.json

# -----------------------------------------------------------------------------
# 4. DEBUG, LOGS, AND LOCAL DATA
# -----------------------------------------------------------------------------

# Logs and Debug Files
# Ignore various package manager and application debug logs.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
firebase-debug.log

# -----------------------------------------------------------------------------
# 5. LOCAL TOOLS AND CONFIGURATION
# -----------------------------------------------------------------------------

# Local Development/Testing
/coverage

# Firebase / Google Cloud Local Files
# Ignore local Firebase configuration and virtual environments.
.firebase
.venv

# IDE and OS Files
# Ignore IDE settings (VS Code) and Operating System metadata (macOS).
.vscode
.DS_Store
*.pem

# Local Artifacts from Cleanup / Temporary Repos
# This is the bare repo created during Git history cleanup and must be ignored.
script-pitcher-final.git